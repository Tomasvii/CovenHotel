Falta hacer un npm init -y
un install Express, un install ejs
un install method-override --save (para poder utilizar put y delete)
y por último npm install -d nodemon

npm i nodemon -D

$ npm i express --save express-validator --save ejs --save express-session --save cookie-parser --save bcryptjs --save method-override --save

para nodemon:
"scripts": {
"start": "nodemon src/app.js"
}

cambiar app.use session key
